# Índice de continuidade

A memória técnica canônica está em `memory/`. Antes de editar, leia todos os arquivos daquele diretório, além de `AGENTS.md`, `README.md` e `ROADMAP.md`.

Este diretório existe para cumprir a convenção operacional `project-memory/` sem duplicar decisões e criar divergência documental.
